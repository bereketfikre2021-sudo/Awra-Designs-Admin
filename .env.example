# Backend API — must include /api suffix
VITE_API_URL=https://awra-designs-backend.onrender.com/api

# Frontend URL (used for "View on site" links)
VITE_FRONTEND_URL=https://awra-designs.netlify.app

# For local dev — leave VITE_API_URL unset, Vite proxy handles /api → localhost:5000
